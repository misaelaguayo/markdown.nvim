#!/usr/bin/env bash
set -e
if [[ ! -d "/Users/misaelaguayo/Projects/markdown.nvim" ]]; then
  echo "Cannot find source directory; Did you move it?"
  echo "(Looking for "/Users/misaelaguayo/Projects/markdown.nvim")"
  echo 'Cannot force reload with this script - use "direnv reload" manually and then try again'
  exit 1
fi

# rebuild the cache forcefully
_nix_direnv_force_reload=1 direnv exec "/Users/misaelaguayo/Projects/markdown.nvim" true

# Update the mtime for .envrc.
# This will cause direnv to reload again - but without re-building.
touch "/Users/misaelaguayo/Projects/markdown.nvim/.envrc"

# Also update the timestamp of whatever profile_rc we have.
# This makes sure that we know we are up to date.
touch -r "/Users/misaelaguayo/Projects/markdown.nvim/.envrc" "/Users/misaelaguayo/Projects/markdown.nvim/.direnv"/*.rc
